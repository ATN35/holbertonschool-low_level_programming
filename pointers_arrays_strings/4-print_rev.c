#include "main.h"

/**
 *print_rev -  a function that prints a string, in reverse
 *@s: s
 *return: Nothing
 */

void print_rev(char *s)
{
	int a;
	for (a = 0; s[a] != 0; a++)
		for (a = a - 1; c >= 0; a--)
			_putchar(s[a]);
	_putchar('\n');
}
