#include "main.h"

/**
 * print_alphabet_x10 - Entry point
 *
 * Return: Nothing
 */
void print_alphabet_x10(void)
{
	int lowercase;
	int i = 0;

	while (i <= 9)
	{
		lowercase = 'a';
		while (lowercase <= 'z')
		{
			_putchar(lowercase);
			lowercase++;
		}
		i++;
		_putchar('\n');
	}
}
